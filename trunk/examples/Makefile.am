# $Id$

CLEANFILES = myapp-history
MAINTAINERCLEANFILES = Makefile.in *~

INCLUDES = -I$(top_builddir)
noinst_PROGRAMS = myapp
myapp_SOURCES = main.cc
myapp_LDADD = $(top_builddir)/libreadline++/libreadline++.la
